# Solo aplica a `npm run dev` (desarrollo con backend en otro puerto).
# En producción (`npm run build`) NO se usa: el frontend queda en same-origin
# porque el backend sirve este build como archivos estáticos.
VITE_API_BASE_URL=http://localhost:4000
